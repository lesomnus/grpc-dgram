{
  "name": "@lesomnus/grpc-dgram",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "gRPC over unreliable datagram channels — TypeScript port of grpc-dgram (wire protocol v1.0)",
  "exports": {
    ".": "./dist/index.mjs",
    "./webrtc": "./dist/webrtc.mjs",
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsdown": "^0.22.5",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
