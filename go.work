go 1.26.1

use (
	.
	./transport/pion
	./transport/gorilla

	// Examples are their own modules so the core never depends on them (and
	// they can pull adapter deps freely); listing them here builds them
	// against this checkout instead of a published version.
	./examples/udp-sensor
	./examples/websocket-echo
	./examples/browser-webrtc
)
